import { MutationTree } from 'vuex'
import { ManagerStateInterface } from './state'

const mutation: MutationTree<ManagerStateInterface> = {
  someMutation (/* state: ExampleStateInterface */) {
    // your code
  }
}

export default mutation
